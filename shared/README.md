# Shared

Shared types, configurations, and utilities used across frontend, backend, and agent.

## Types

TypeScript type definitions that are shared between frontend and backend to ensure type safety across the stack.
